spring:
  application:
    name: enterprise-chatbot-platform
  
  datasource:
    url: jdbc:mysql://localhost:3306/chatbot_platform?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  
  security:
    user:
      name: admin
      password: admin

server:
  port: 8080
  servlet:
    context-path: /api

# JWT Configuration
jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
  expiration: 86400000  # 24 hours in milliseconds

# Gemini AI Configuration
gemini:
  api:
    key: ${GEMINI_API_KEY:AIzaSyBdYicZTEnrfBfuRAZ5RMbFT5HJAqw4Pws}
    url: https://generativelanguage.googleapis.com/v1beta/models/gemini-pro:generateContent
    model: gemini-pro

# Application Configuration
app:
  retrieval:
    threshold: 0.7
    max-results: 5
  ai:
    confidence-threshold: 0.6
    max-tokens: 500
